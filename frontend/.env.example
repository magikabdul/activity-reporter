# Dev-only: where the Vite dev server proxies /reporter requests.
# Default is the cluster ingress. Alternative: kubectl -n krisoo port-forward svc/reporter-service 7500:7500
VITE_API_PROXY=http://10.78.20.201
